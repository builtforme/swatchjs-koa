/*
const chai = require('chai');

const validation = require('../lib/validation');

const expect = chai.expect;
*/
describe('validation', () => {
  it('should wrap validation functions and handle exceptions', () => {

  });
});
