/*
const chai = require('chai');

const middleware = require('../lib/middleware');

const expect = chai.expect;
*/
describe('middleware', () => {
  it('should initialize the swatchCtx', () => {

  });

  it('should wrap middleware functions and handle exceptions', () => {

  });
});
